schema_id = "ff.architecture_policy@2"
file_id = "FF-GOV-BUILD-ARCH-POLICY-001"
schema_version = "2.0.0"
accepted_design_decision_ids = ["FF-BUILD-036", "FF-BUILD-077"]
workspace_manifest = "build/Cargo.toml"
workspace_root = "build"
target_directory = "build/target"
root_toolchain_selector = "rust-toolchain.toml"
governance_root = ".GOV"
product_root = "product"
build_root = "build"
exception_authority = ".GOV/rules/build-rules.yaml"
exception_decision_ids = []
unsafe_policy = "forbid_in_workspace_members"
unsafe_exception_authority = ".GOV/rules/build-rules.yaml#exception_authority"
unsafe_decision_ids = []
internal_edge_default = "deny"
forbidden_layer_edges = ["engine->frontend", "engine->adapter", "engine->watcher", "adapter->adapter", "product->watcher", "watcher->engine", "normal_or_build:product->testkit"]
forbidden_product_runtime_roots = [".GOV", "build"]
allowed_runtime_or_native_dependencies = ["FFmpeg", "ffprobe"]
approved_transitive_build_packages = ["camino@1.2.4", "crossbeam-utils@0.8.22", "getrandom@0.3.4", "icu_normalizer_data@2.0.0", "icu_properties_data@2.0.1", "libc@0.2.189", "memoffset@0.9.1", "ntapi@0.4.3", "num-traits@0.2.19", "parking_lot_core@0.9.12", "paste@1.0.15", "portable-atomic@1.14.0", "proc-macro2@1.0.107", "quote@1.0.47", "rustversion@1.0.23", "serde@1.0.228", "serde_core@1.0.228", "serde_json@1.0.150", "thiserror@2.0.19", "winapi-i686-pc-windows-gnu@0.4.0", "winapi-x86_64-pc-windows-gnu@0.4.0", "winapi@0.3.9", "wit-bindgen@0.57.1", "zerocopy@0.8.55", "zmij@1.0.23"]
approved_transitive_proc_macros = ["boa_macros@0.21.1", "bytemuck_derive@1.11.0", "derive_arbitrary@1.4.2", "displaydoc@0.2.6", "dynify-macros@0.1.2", "equator-macro@0.4.2", "num_enum_derive@0.7.6", "paste@1.0.15", "phf_macros@0.13.1", "pin-project-internal@1.1.13", "rustversion@1.0.23", "serde_derive@1.0.228", "thiserror-impl@2.0.19", "time-macros@0.2.32", "windows-implement@0.60.2", "windows-interface@0.59.3", "yoke-derive@0.8.2", "zerocopy-derive@0.8.55", "zerofrom-derive@0.1.7", "zerovec-derive@0.11.3"]

[[members]]
name = "fforager-xtask"
manifest = "build/tools/fforager-xtask/Cargo.toml"
source_root = "build/tools/fforager-xtask/src"
layer = "build_tooling"
artifact_role = "non_shipped_gate_runner"
shipped = false
test_only = false
publish_allowed = false
allowed_internal_dependencies = []
split_trigger = "FF-BUILD-036"
feature_owner = "WP-FF-003-executable-gate-bootstrap-v1"
profile = "phase0_bootstrap"
removal_condition = "Replace only through a versioned gate-runner migration that preserves all report and proof contracts."
runtime_native_constraint_ref = "FF-START-BOUNDARY-001"
unsafe_policy_ref = "FF-BUILD-050"
exception_policy_ref = "FF-BUILD-052"

[[members]]
name = "fforager-javascript"
manifest = "build/crates/fforager-javascript/Cargo.toml"
source_root = "build/crates/fforager-javascript/src"
layer = "build_tooling"
artifact_role = "non_shipped_isolated_javascript_worker_spike"
shipped = false
test_only = false
publish_allowed = false
allowed_internal_dependencies = [{ package = "fforager-contracts", kinds = ["normal"], optional = false }]
split_trigger = "WP-FF-006-rust-youtube-challenge-spike-v1-AC-001"
feature_owner = "WP-FF-006-rust-youtube-challenge-spike-v1"
profile = "phase0_prerequisite_spike"
removal_condition = "Remove or replace only after the Rust JavaScript approach receives an explicit Phase-0 viability verdict and any successor preserves the bounded worker proof contract."
runtime_native_constraint_ref = "FF-START-BOUNDARY-001"
unsafe_policy_ref = "FF-BUILD-050"
exception_policy_ref = "FF-BUILD-052"

[[members]]
name = "fforager-transport"
manifest = "build/crates/fforager-transport/Cargo.toml"
source_root = "build/crates/fforager-transport/src"
layer = "build_tooling"
artifact_role = "non_shipped_transport_capability_spike"
shipped = false
test_only = false
publish_allowed = false
allowed_internal_dependencies = [{ package = "fforager-contracts", kinds = ["normal"], optional = false }]
split_trigger = "WP-FF-007-transport-fingerprint-spike-v1-AC-002"
feature_owner = "WP-FF-007-transport-fingerprint-spike-v1"
profile = "phase0_prerequisite_spike"
removal_condition = "Remove or replace only after WP-007 records a bounded transport verdict and any successor preserves the mandatory corpus and fail-closed capability contract."
runtime_native_constraint_ref = "FF-START-BOUNDARY-001"
unsafe_policy_ref = "FF-BUILD-050"
exception_policy_ref = "FF-BUILD-052"

[[members]]
name = "fforager-contracts"
manifest = "product/crates/fforager-contracts/Cargo.toml"
source_root = "product/crates/fforager-contracts/src"
layer = "contracts"
artifact_role = "shipped_versioned_wire_contracts"
shipped = true
test_only = false
publish_allowed = false
allowed_internal_dependencies = []
split_trigger = "WP-FF-005-versioned-core-contracts-v1-AC-001"
feature_owner = "WP-FF-005-versioned-core-contracts-v1"
profile = "phase0_prerequisite_contracts"
removal_condition = "Replace only through a versioned contract migration with retained prior-version fixtures and conformance proof."
runtime_native_constraint_ref = "FF-START-BOUNDARY-001"
unsafe_policy_ref = "FF-BUILD-050"
exception_policy_ref = "FF-BUILD-052"

[[members]]
name = "fforager-diagnostics-contract"
manifest = "product/crates/fforager-diagnostics-contract/Cargo.toml"
source_root = "product/crates/fforager-diagnostics-contract/src"
layer = "contracts"
artifact_role = "shipped_bounded_diagnostic_contracts"
shipped = true
test_only = false
publish_allowed = false
allowed_internal_dependencies = []
split_trigger = "WP-FF-005-versioned-core-contracts-v1-AC-001"
feature_owner = "WP-FF-005-versioned-core-contracts-v1"
profile = "phase0_prerequisite_contracts"
removal_condition = "Replace only through a versioned diagnostic migration preserving bounded payload and prior-version conformance proof."
runtime_native_constraint_ref = "FF-START-BOUNDARY-001"
unsafe_policy_ref = "FF-BUILD-050"
exception_policy_ref = "FF-BUILD-052"

[[members]]
name = "fforager-core"
manifest = "product/crates/fforager-core/Cargo.toml"
source_root = "product/crates/fforager-core/src"
layer = "engine"
artifact_role = "shipped_deterministic_core_models"
shipped = true
test_only = false
publish_allowed = false
allowed_internal_dependencies = [{ package = "fforager-contracts", kinds = ["normal"], optional = false }]
split_trigger = "WP-FF-005-versioned-core-contracts-v1-AC-003"
feature_owner = "WP-FF-005-versioned-core-contracts-v1"
profile = "phase0_prerequisite_contracts"
removal_condition = "Replace only through a versioned deterministic-model migration with state, resource, durability, and replay proof."
runtime_native_constraint_ref = "FF-START-BOUNDARY-001"
unsafe_policy_ref = "FF-BUILD-050"
exception_policy_ref = "FF-BUILD-052"

[[members]]
name = "fforager-testkit"
manifest = "build/crates/fforager-testkit/Cargo.toml"
source_root = "build/crates/fforager-testkit/src"
layer = "testkit"
artifact_role = "non_shipped_contract_conformance_harness"
shipped = false
test_only = true
publish_allowed = false
allowed_internal_dependencies = [
  { package = "fforager-contracts", kinds = ["normal"], optional = false },
  { package = "fforager-diagnostics-contract", kinds = ["normal"], optional = false },
  { package = "fforager-core", kinds = ["normal"], optional = false },
]
split_trigger = "WP-FF-005-versioned-core-contracts-v1-AC-005"
feature_owner = "WP-FF-005-versioned-core-contracts-v1"
profile = "phase0_prerequisite_contracts"
removal_condition = "Remove only after every framed protocol consumer has equivalent shared prior/current and negative-path conformance proof."
runtime_native_constraint_ref = "FF-START-BOUNDARY-001"
unsafe_policy_ref = "FF-BUILD-050"
exception_policy_ref = "FF-BUILD-052"

[[dependency_decisions]]
name = "cargo_metadata"
version = "0.23.1"
consumer = "fforager-xtask"
runtime_class = "non_shipped_build_tooling"
purpose = "Consume Cargo metadata format 1 without a brittle hand-written JSON parser."
native = false
owner = "WP-FF-003-executable-gate-bootstrap"
allowed_consumers = ["fforager-xtask"]
reason = "The official structured Cargo graph surface avoids lossy cargo-tree text parsing."
removal_trigger = "Cargo exposes an equally stable std-only metadata interface or the gate runner is replaced through a validated migration."
approval_id = "WP-FF-003-executable-gate-bootstrap-v1-AC-002"
features = []
default_features = false

[[dependency_decisions]]
name = "serde_json"
version = "1.0.150"
consumer = "fforager-testkit"
runtime_class = "non_shipped_test_tooling"
purpose = "Load and validate canonical contract inventory and cross-version JSON fixtures."
native = false
owner = "WP-FF-005-versioned-core-contracts"
allowed_consumers = ["fforager-testkit"]
reason = "The conformance harness must replay the same JSON artifacts used by shipped contract readers."
removal_trigger = "All registered wire contracts migrate away from JSON through accepted compatibility fixtures."
approval_id = "WP-FF-005-versioned-core-contracts-v1-AC-005"
features = []
default_features = true

[[dependency_decisions]]
name = "serde"
version = "1.0.228"
consumer = "fforager-contracts"
runtime_class = "shipped_rust_product"
purpose = "Serialize and deserialize explicit versioned wire DTOs."
native = false
owner = "WP-FF-005-versioned-core-contracts"
allowed_consumers = ["fforager-contracts"]
reason = "The contract boundary requires a mature typed serializer with deny-unknown-fields and exact pinned behavior."
removal_trigger = "All versioned wire contracts migrate through accepted fixtures to an equally strict serializer."
approval_id = "WP-FF-005-versioned-core-contracts-v1-AC-001"
features = ["derive"]
default_features = true

[[dependency_decisions]]
name = "serde_json"
version = "1.0.150"
consumer = "fforager-contracts"
runtime_class = "shipped_rust_product"
purpose = "Encode and decode bounded JSON wire envelopes and fixture payloads."
native = false
owner = "WP-FF-005-versioned-core-contracts"
allowed_consumers = ["fforager-contracts"]
reason = "The accepted WP-005 wire fixtures and framing contract use JSON payloads."
removal_trigger = "The wire format migrates through a versioned compatibility plan and retained prior-version fixtures."
approval_id = "WP-FF-005-versioned-core-contracts-v1-AC-001"
features = []
default_features = true

[[dependency_decisions]]
name = "serde"
version = "1.0.228"
consumer = "fforager-diagnostics-contract"
runtime_class = "shipped_rust_product"
purpose = "Serialize and deserialize bounded versioned diagnostic DTOs."
native = false
owner = "WP-FF-005-versioned-core-contracts"
allowed_consumers = ["fforager-diagnostics-contract"]
reason = "Diagnostic producers and consumers require a shared strict typed contract."
removal_trigger = "All diagnostic contracts migrate through accepted fixtures to an equally strict serializer."
approval_id = "WP-FF-005-versioned-core-contracts-v1-AC-001"
features = ["derive"]
default_features = true

[[dependency_decisions]]
name = "serde_json"
version = "1.0.150"
consumer = "fforager-diagnostics-contract"
runtime_class = "shipped_rust_product"
purpose = "Encode and decode bounded diagnostic payloads and fixtures."
native = false
owner = "WP-FF-005-versioned-core-contracts"
allowed_consumers = ["fforager-diagnostics-contract"]
reason = "The accepted diagnostic wire representation and conformance fixtures use JSON."
removal_trigger = "The diagnostic wire format migrates through a versioned compatibility plan and retained prior-version fixtures."
approval_id = "WP-FF-005-versioned-core-contracts-v1-AC-001"
features = []
default_features = true

[[dependency_decisions]]
name = "serde"
version = "1.0.228"
consumer = "fforager-xtask"
runtime_class = "non_shipped_build_tooling"
purpose = "Strict typed policy and report serialization."
native = false
owner = "WP-FF-003-executable-gate-bootstrap"
allowed_consumers = ["fforager-xtask"]
reason = "Typed deny-unknown-fields schemas are required for fail-closed project policy parsing."
removal_trigger = "All governed policy and report surfaces are removed or migrate to an equally strict approved serializer."
approval_id = "WP-FF-003-executable-gate-bootstrap-v1-AC-002"
features = ["derive"]
default_features = true

[[dependency_decisions]]
name = "serde_json"
version = "1.0.150"
consumer = "fforager-xtask"
runtime_class = "non_shipped_build_tooling"
purpose = "Parse Cargo, packet, and report JSON."
native = false
owner = "WP-FF-003-executable-gate-bootstrap"
allowed_consumers = ["fforager-xtask"]
reason = "Cargo metadata, packet contracts, and gate reports use JSON as their accepted machine-readable format."
removal_trigger = "Every governed JSON producer and consumer migrates through a versioned accepted contract."
approval_id = "WP-FF-003-executable-gate-bootstrap-v1-AC-002"
features = []
default_features = true

[[dependency_decisions]]
name = "saphyr"
version = "0.0.11"
consumer = "fforager-xtask"
runtime_class = "non_shipped_build_tooling"
purpose = "Parse governance YAML fail-closed before consuming build rules or active task state."
native = false
owner = "WP-FF-003-executable-gate-bootstrap"
allowed_consumers = ["fforager-xtask"]
reason = "A maintained YAML 1.2 parser is safer and smaller than inventing a project-local parser for authoritative governance files."
removal_trigger = "Governance authority migrates away from YAML or Rust gains an approved equivalent parser already present in the workspace."
approval_id = "WP-FF-003-executable-gate-bootstrap-v2-AC-003"
features = []
default_features = false

[[dependency_decisions]]
name = "sha2"
version = "0.11.0"
consumer = "fforager-xtask"
runtime_class = "non_shipped_build_tooling"
purpose = "Verify SHA-256 identities for host-installed validation executables."
native = false
owner = "WP-FF-003-executable-gate-bootstrap"
allowed_consumers = ["fforager-xtask"]
reason = "FF-BUILD-047 requires exact checksum evidence for tools not owned by the pinned Rust toolchain or workspace lockfile."
removal_trigger = "All validation executables are supplied through an independently verified toolchain or a standard-library SHA-256 implementation becomes available."
approval_id = "WP-FF-003-executable-gate-bootstrap-v2-AC-003"
features = []
default_features = false

[[dependency_decisions]]
name = "toml"
version = "1.1.3"
consumer = "fforager-xtask"
runtime_class = "non_shipped_build_tooling"
purpose = "Strict parsing of project-owned TOML policy."
native = false
owner = "WP-FF-003-executable-gate-bootstrap"
allowed_consumers = ["fforager-xtask"]
reason = "Cargo and project build policies use TOML and must be parsed semantically rather than through lexical matching."
removal_trigger = "All governed TOML policy migrates through a versioned accepted contract."
approval_id = "WP-FF-003-executable-gate-bootstrap-v1-AC-002"
features = []
default_features = true

[[dependency_decisions]]
name = "boa_ast"
version = "0.21.1"
consumer = "fforager-javascript"
runtime_class = "non_shipped_build_tooling"
purpose = "Inspect parsed player syntax structurally and retain only Ferric-selected challenge definitions."
native = false
owner = "WP-FF-006-rust-youtube-challenge-spike"
allowed_consumers = ["fforager-javascript"]
reason = "The spike requires AST-level discovery rather than executing a whole site-supplied player or importing external solver code."
removal_trigger = "The spike is removed or a validated Ferric-owned parser provides equivalent bounded structural discovery."
approval_id = "WP-FF-006-rust-youtube-challenge-spike-v1-AC-003"
features = []
default_features = true

[[dependency_decisions]]
name = "boa_engine"
version = "0.21.1"
consumer = "fforager-javascript"
runtime_class = "non_shipped_build_tooling"
purpose = "Execute reduced site-supplied challenge behavior in a fresh pure-Rust context with explicit runtime limits."
native = false
owner = "WP-FF-006-rust-youtube-challenge-spike"
allowed_consumers = ["fforager-javascript"]
reason = "Boa is a current pure-Rust ECMAScript engine with instruction, loop, recursion, and stack limit controls suitable for the isolated spike."
removal_trigger = "The spike is removed or an independently validated pure-Rust engine provides stronger equivalent bounded execution."
approval_id = "WP-FF-006-rust-youtube-challenge-spike-v1-AC-003"
features = ["fuzz"]
default_features = false

[[dependency_decisions]]
name = "boa_gc"
version = "0.21.1"
consumer = "fforager-javascript"
runtime_class = "non_shipped_build_tooling"
purpose = "Force collection after each discarded per-job context so same-worker RSS remains inside the protocol ceiling."
native = false
owner = "WP-FF-006-rust-youtube-challenge-spike"
allowed_consumers = ["fforager-javascript"]
reason = "The engine context is fresh per job, but deterministic collection is required to bound retained process memory between jobs."
removal_trigger = "Boa exposes equivalent deterministic context teardown or the isolated engine is replaced through a validated migration."
approval_id = "WP-FF-006-rust-youtube-challenge-spike-v1-AC-003"
features = []
default_features = true

[[dependency_decisions]]
name = "boa_interner"
version = "0.21.1"
consumer = "fforager-javascript"
runtime_class = "non_shipped_build_tooling"
purpose = "Resolve interned AST identifiers during Ferric-owned player challenge discovery."
native = false
owner = "WP-FF-006-rust-youtube-challenge-spike"
allowed_consumers = ["fforager-javascript"]
reason = "Boa AST nodes store identifiers through the matching interner and discovery must compare their exact source identities."
removal_trigger = "The selected parser exposes stable owned identifiers without this direct dependency or the spike is removed."
approval_id = "WP-FF-006-rust-youtube-challenge-spike-v1-AC-003"
features = []
default_features = true

[[dependency_decisions]]
name = "boa_parser"
version = "0.21.1"
consumer = "fforager-javascript"
runtime_class = "non_shipped_build_tooling"
purpose = "Parse committed raw player JavaScript into the structural discovery input."
native = false
owner = "WP-FF-006-rust-youtube-challenge-spike"
allowed_consumers = ["fforager-javascript"]
reason = "Using the engine-aligned parser avoids brittle regular-expression extraction and preserves a single pinned syntax model."
removal_trigger = "The spike is removed or a validated Ferric-owned parser supplies equivalent current-player coverage."
approval_id = "WP-FF-006-rust-youtube-challenge-spike-v1-AC-003"
features = []
default_features = true

[[dependency_decisions]]
name = "serde"
version = "1.0.228"
consumer = "fforager-javascript"
runtime_class = "non_shipped_build_tooling"
purpose = "Strictly serialize the worker request, response, corpus manifest, and proof report DTOs."
native = false
owner = "WP-FF-006-rust-youtube-challenge-spike"
allowed_consumers = ["fforager-javascript"]
reason = "The spike reuses the WP-005 JSON envelope and requires deny-unknown-fields DTO validation."
removal_trigger = "The worker boundary migrates through accepted compatibility fixtures away from Serde."
approval_id = "WP-FF-006-rust-youtube-challenge-spike-v1-AC-003"
features = ["derive"]
default_features = true

[[dependency_decisions]]
name = "serde_json"
version = "1.0.150"
consumer = "fforager-javascript"
runtime_class = "non_shipped_build_tooling"
purpose = "Encode and decode bounded WP-005 worker envelopes, manifests, and machine proof reports."
native = false
owner = "WP-FF-006-rust-youtube-challenge-spike"
allowed_consumers = ["fforager-javascript"]
reason = "The accepted versioned JavaScript worker contract and committed corpus use strict JSON representations."
removal_trigger = "The worker protocol and retained corpus migrate through an accepted non-JSON compatibility plan."
approval_id = "WP-FF-006-rust-youtube-challenge-spike-v1-AC-003"
features = []
default_features = true

[[dependency_decisions]]
name = "sha2"
version = "0.11.0"
consumer = "fforager-javascript"
runtime_class = "non_shipped_build_tooling"
purpose = "Verify raw-player identity and bind cache/report evidence to exact source bytes."
native = false
owner = "WP-FF-006-rust-youtube-challenge-spike"
allowed_consumers = ["fforager-javascript"]
reason = "The corpus contract requires exact SHA-256 provenance before any site-supplied player code is parsed or evaluated."
removal_trigger = "Rust supplies a standard SHA-256 implementation or the corpus identity contract migrates through an accepted decision."
approval_id = "WP-FF-006-rust-youtube-challenge-spike-v1-AC-002"
features = []
default_features = false

[[dependency_decisions]]
name = "sysinfo"
version = "0.39.6"
consumer = "fforager-javascript"
runtime_class = "non_shipped_build_tooling"
purpose = "Measure worker RSS and prove termination, reaping, and process absence from the supervising Rust process."
native = false
owner = "WP-FF-006-rust-youtube-challenge-spike"
allowed_consumers = ["fforager-javascript"]
reason = "Boa has no exact heap quota, so the WP-005 process memory ceiling requires an independent parent-side RSS supervisor."
removal_trigger = "The selected engine exposes a validated exact memory cap or the operating-system supervisor migrates to an approved equivalent."
approval_id = "WP-FF-006-rust-youtube-challenge-spike-v1-AC-004"
features = ["system"]
default_features = false

[[dependency_decisions]]
name = "serde"
version = "1.0.228"
consumer = "fforager-transport"
runtime_class = "non_shipped_build_tooling"
purpose = "Strictly serialize the WP-007 corpus manifest, case evidence, and aggregate verdict."
native = false
owner = "WP-FF-007-transport-fingerprint-spike"
allowed_consumers = ["fforager-transport"]
reason = "The mandatory corpus requires deny-unknown-fields DTOs and deterministic machine-readable reports."
removal_trigger = "The non-shipped spike is removed or its retained corpus migrates through an accepted non-Serde format transition."
approval_id = "WP-FF-007-transport-fingerprint-spike-v1-AC-002"
features = ["derive"]
default_features = true

[[dependency_decisions]]
name = "serde_json"
version = "1.0.150"
consumer = "fforager-transport"
runtime_class = "non_shipped_build_tooling"
purpose = "Decode the strict transport corpus and encode deterministic behavioral evidence."
native = false
owner = "WP-FF-007-transport-fingerprint-spike"
allowed_consumers = ["fforager-transport"]
reason = "WP-004 normalization conventions and existing Ferric proof surfaces use bounded JSON."
removal_trigger = "The retained corpus and report migrate through an accepted non-JSON compatibility transition."
approval_id = "WP-FF-007-transport-fingerprint-spike-v1-AC-002"
features = []
default_features = true

[[dependency_decisions]]
name = "sha2"
version = "0.11.0"
consumer = "fforager-transport"
runtime_class = "non_shipped_build_tooling"
purpose = "Bind manifest, transcript, and candidate evidence to deterministic SHA-256 identities."
native = false
owner = "WP-FF-007-transport-fingerprint-spike"
allowed_consumers = ["fforager-transport"]
reason = "The spike must prove exact corpus and wire-evidence identities using the already-approved repository digest implementation."
removal_trigger = "Rust supplies a standard SHA-256 implementation or the retained evidence contract migrates through an accepted digest change."
approval_id = "WP-FF-007-transport-fingerprint-spike-v1-AC-002"
features = []
default_features = false
